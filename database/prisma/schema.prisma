generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Project {
  id    String @id @default(cuid())
  name  String?
  color String
  tasks Task[]
}

model Task {
  id            String          @id @default(cuid())
  name          String?
  project       Project         @relation(fields: [projectId], references: [id], onDelete: Cascade)
  projectId     String
  start         DateTime?
  end           DateTime?
  duration      Int?
  cost          Float?
  predecessors  TaskHierarchy[] @relation("PredecessorRelation")
  successors    TaskHierarchy[] @relation("SuccessorRelation")
  importance    Importance      @default(medium)
  weatherEffect WeatherEffect   @default(none)
}

model TaskHierarchy {
  predecessorId String
  successorId   String
  predecessor   Task            @relation("PredecessorRelation", fields: [predecessorId], references: [id])
  successor     Task            @relation("SuccessorRelation", fields: [successorId], references: [id])

  @@id([predecessorId, successorId])
}


enum Importance {
  asap
  high
  medium
  low
}

enum WeatherEffect {
  none
  slight
  significant
  impossible
}
